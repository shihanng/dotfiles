---
- name: Install stuffs
  aur:
    name:
      - "google-chrome"
      - "pavucontrol"
      - bitwarden-bin
      - spotify
      - keybase-bin
    use: yay
    skip_installed: yes
  become: yes
  become_user: "{{ aur_user }}"
