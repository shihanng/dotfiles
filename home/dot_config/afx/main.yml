github:
  - name: sops
    owner: mozilla
    repo: sops
    release:
      name: sops
      tag: v3.7.3
    command:
      link:
        - from: "sops*"
          to: sops

  - name: kind
    owner: kubernetes-sigs
    repo: kind
    release:
      name: kind
      tag: v0.17.0
    command:
      link:
        - from: "kind*"
          to: kind

  - name: kustomize
    owner: kubernetes-sigs
    repo: kustomize
    release:
      name: kustomize
      tag: kustomize/v4.5.7
    command:
      link:
        - from: "kustomize*"
          to: kustomize

http:
- name: terraform
  url: "https://releases.hashicorp.com/terraform/1.3.7/terraform_1.3.7_{{ .OS }}_{{ .Arch }}.zip"
  command:
    link:
    - from: terraform
